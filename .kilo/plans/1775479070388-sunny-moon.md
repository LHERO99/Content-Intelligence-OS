# Plan: Fix Content Monitoring API 500 Error and Verification

The user is reporting a 500 error on `/api/monitoring`. Based on the server logs, the code is executing the cost calculation logic but failing somewhere during or after the loop.

# Plan: Fix TypeError in Content Monitoring API

The user is encountering a `TypeError: s.toLowerCase is not a function` in the `/api/monitoring` route. Based on the stack trace and the code, this likely occurs when attempting to call `.toLowerCase()` on a variable that is not a string (e.g., `null`, `undefined`, or an array) within the `costs.find` or `counts` key generation logic.

## Analysis
The error happens in `src/app/api/monitoring/route.ts` during the `forEach` loop on `urlLogMap`. Specifically, these locations are vulnerable:
1.  Line 132-133: `c.Page_Type?.toLowerCase()` and `pageType.toLowerCase()`.
2.  Line 142-143: `pageType.toLowerCase()` and `actionType.toLowerCase()`.

In Airtable, fields that are supposed to be single selects or strings can sometimes return as arrays or null if not handled correctly by the library.

## Proposed Steps

### 1. Robust Type Casting and Null Checks
- Ensure `pageType` and `actionType` are strings before calling `.toLowerCase()`.
- Use helper functions or explicit checks to handle cases where these values might be missing or non-string types.
- Apply similar fixes to `src/app/api/monitoring/detail/route.ts` as it contains identical logic.

### 2. Implementation
- Edit `src/app/api/monitoring/route.ts`:
    - Wrap `pageType` and `actionType` in `String()` or add null checks before `.toLowerCase()`.
    - Ensure `c.Page_Type` and `c.Action_Type` from the `costs` array are also safely handled.
- Edit `src/app/api/monitoring/detail/route.ts`:
    - Apply the same robust string handling.

### 3. Verification
- Since I cannot run the server, I will rely on `npm run build` to catch type-level issues (though this is a runtime error).
- The added `console.error` in the previous turn will help identify if other parts of the data are malformed.

## Review Required
- None, this is a standard bug fix for runtime safety.

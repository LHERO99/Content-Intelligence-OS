# Plan: Fix Commissioning Error in Airtable

The user reports a 422 error from Airtable when commissioning content. The n8n issue is already resolved by the user.

## Root Cause Analysis
1. **Airtable 422 Error**: The `Content-Log` table in Airtable has a field `Target_URL` which is a "computed" field (likely a Lookup or Formula). The code in `src/lib/airtable.ts` (specifically `createContentLog`) attempts to write to this field, which Airtable rejects with "Field Target_URL cannot accept a value because the field is computed".

## Proposed Changes

### 1. Fix Airtable Logging (`src/lib/airtable.ts`)
- Modify `createContentLog` to remove `Target_URL` from the fields being sent to Airtable, as it is automatically computed based on the linked `Keyword_ID`.

### 2. Robust API Handling (`src/app/api/planning/keywords/route.ts`)
- The calls to `createContentLog` in the API route already pass `Target_URL`. I will ensure the underlying service handles this gracefully by simply not sending it to Airtable.

## Step-by-Step Execution Plan
1. **Fix `createContentLog`**: Update the `createContentLog` function in `src/lib/airtable.ts` to exclude the computed `Target_URL` field during the `base(...).create()` call.
2. **Verification**: Confirm that other fields like `Keyword_ID` and `Diff_Summary` are still correctly sent.

I will proceed with the Airtable fix now.

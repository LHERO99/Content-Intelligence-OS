# Plan: Implement Blacklist Logic for Main Keywords and URL Display Fix

This plan addresses several requirements:
1.  Implement double confirmation and data loss warning for URL-level blacklisting. (Completed)
2.  Prevent blacklisting of a Main Keyword unless the entire URL is blacklisted or a new Main Keyword is assigned. (Completed)
3.  Fix the "Blacklisted" flag in the URL history to only appear when the URL itself is blacklisted. (Completed)
4.  Fix the URL display in the Blacklist modal to ensure long URLs wrap correctly and stay within the modal's boundaries.

## Proposed Changes

### 1. `src/features/planning/components/blacklist-reason-modal.tsx`

#### UI Styling Update
-   Modify the `li` element in the affected keywords/URLs list.
-   Remove the `truncate` class which hides long URLs.
-   Add `break-all` or `break-words` class to ensure long URLs wrap.
-   Ensure the `ul` container allows for natural height adjustment while maintaining its `max-h-32` and `overflow-y-auto` behavior.

## Verification Plan

1.  **Test: Long URL Display**
    *   Select a keyword with a very long Target URL.
    *   Open the Blacklist modal and select type "URL".
    *   **Expected**: The URL should wrap to multiple lines and remain within the modal's width, rather than overflowing or being cut off by `truncate`.


# Plan: System-Zustand und DB-Migrationen prüfen

Dieses Dokument beschreibt die nächsten Schritte zur Validierung des System-Zustands und zur Durchführung ausstehender Datenbank-Migrationen, basierend auf dem aktuellen Stand der `docs/memory/` Dokumentation.

## Aktueller Status
Zwei neue Bugs wurden identifiziert:
1. Nach der Beauftragung im Redaktionsplan verschwindet der Eintrag nicht.
2. Nach dem Markieren als "Veröffentlicht" in der Content-Erstellung verschwindet die Vorschau und der Ladespinner erscheint wieder.

Das System zeigt ein fehlerhaftes Verhalten beim Workflow von der Vorschlagsliste bis zur Content-Anlieferung, insbesondere bei Optimierungen. Der aktuelle Prozess überspringt teilweise Schritte oder führt zu inkonsistenten Zuständen in der Auftrags-Historie.

## Gewünschter Workflow
1.  **Vorschlag:** Thema erscheint im Tab "Vorschläge" (Typ: "Erstellung" oder "Optimierung").
2.  **Planung:** Durch Klick auf "Hinzufügen" wandert das Thema in den Tab "Redaktionsplan" (Status: `Planned`). Der Typ ("Erstellung" oder "Optimierung") wird dabei explizit übernommen.
3.  **Beauftragung:** Durch Klick auf "Beauftragen" im Redaktionsplan wird der Prozess gestartet (Status: `Beauftragt`). Das Thema verschwindet aus dem Redaktionsplan.
4.  **Content-Erstellung:** Das Thema erscheint in der Liste "Content-Erstellung" mit dem korrekten Typ.
5.  **Anlieferung:** Nach Anlieferung (Status: `Angeliefert`) und Veröffentlichung (Status: `Published`) kann das Thema später erneut zur Optimierung vorgeschlagen werden.
6.  **Re-Zirkulation:** Ein optimiertes Thema muss denselben Workflow durchlaufen (Vorschläge -> Redaktionsplan -> Beauftragen -> Anlieferung) und dabei einen **neuen** Log-Eintrag in der Historie erzeugen, statt den alten zu überschreiben.

---

## Zyklus-Isolation: Status und Content pro Beauftragungszyklus

### Problem

Aktuell teilen alle Aufträge eines Keywords (`keyword_id`) dieselbe Datenbasis:

1. **Status-Problem:** `JobEntry.keywordStatus` stammt aus `kw.Status` (Keyword-Record). Wird ein Auftrag als "Veröffentlicht" markiert, setzt `handlePublish` → `PlanningService.updateKeyword(keywordId, { Status: 'Published' })` → `kw.Status = 'Published'` für das gesamte Keyword. Damit erscheinen **alle** historischen Aufträge desselben Keywords als "Veröffentlicht".

2. **Content-Problem:** `handleSaveContent` und `handleSaveFromAI` in `ai-editor-workspace.tsx` rufen `POST /api/planning/history` mit nur `keywordId` auf. Die neue Content-Log-Zeile ist dadurch mit keinem bestimmten Zyklus verknüpft. `buildJobEntries` bestimmt den Delivery-Log eines Zyklus temporal (nächster v2-Log nach Beauftragungszeitpunkt) — ein neu gespeicherter Content erscheint dadurch im falschen Zyklus oder überschreibt die Zuordnung.

### Root Cause

Es gibt keinen persistenten Verweis, der einen `content_log`-Eintrag (Delivery, Manuelle Anpassung, KI-Save, Veröffentlichung) mit seiner **übergeordneten Beauftragung** verknüpft.

### Lösung: `commission_log_id` auf `content_log`

Ein neues optionales Feld `commission_log_id INTEGER REFERENCES content_log(id) ON DELETE SET NULL` auf der `content_log`-Tabelle verankert jeden Log-Eintrag an seinen Beauftragungszyklus.

---

### Schritt 1 — DB-Migration

**Neue Datei:** `src/lib/db/migrations/0005_add_commission_log_id.sql`
```sql
ALTER TABLE "content_log"
  ADD COLUMN "commission_log_id" integer REFERENCES "content_log"("id") ON DELETE SET NULL;

CREATE INDEX "content_log_commission_idx" ON "content_log"("commission_log_id");
```

**Manuell gegen DB ausführen** (analog zu Migration 0004).

---

### Schritt 2 — Drizzle Schema

**Datei:** `src/lib/db/schema.ts`

Zum `contentLog`-Table hinzufügen:
```ts
commissionLogId: integer('commission_log_id').references((): AnyPgColumn => contentLog.id, { onDelete: 'set null' }),
```

(Self-referencing FK — benötigt `AnyPgColumn` aus `drizzle-orm/pg-core`.)

---

### Schritt 3 — TypeScript-Interface

**Datei:** `src/lib/postgres-types.ts`

`ContentLog` bekommt:
```ts
Commission_Log_Id?: number;
```

---

### Schritt 4 — `createContentLog` erweitern

**Datei:** `src/lib/postgres.ts`

`createContentLog` akzeptiert neues optionales Feld `Commission_Log_Id?: number` in `Partial<ContentLog>`:
```ts
// in INSERT .values({ ... })
commissionLogId: log.Commission_Log_Id ?? null,
```

`mapContentLogRow` gibt es zurück:
```ts
Commission_Log_Id: row.commissionLogId ?? undefined,
```

---

### Schritt 5 — Trigger-Route: Commission-Log-ID in Payload

**Datei:** `src/app/api/agent-webhook/trigger/route.ts`

Der `createContentLog`-Aufruf für `"Content wurde beauftragt"` gibt bereits `newLog` zurück. Die ID muss in `enrichedPayload` übergeben werden:

```ts
const commissionLog = await createContentLog({ ... });

const enrichedPayload = {
  ...
  commissionLogId: commissionLog?.ID ?? null,   // ← neu
};
```

---

### Schritt 6 — Callback-Route: commission_log_id beim Delivery setzen

**Datei:** `src/app/api/agent-webhook/callback/route.ts`

Der Body des Callbacks enthält nun `commissionLogId`. Der `createContentLog`-Aufruf setzt es:

```ts
const newLog = await createContentLog({
  ...
  Commission_Log_Id: body.commissionLogId ?? null,
}, tenantId);
```

---

### Schritt 7 — `AIEditorWorkspace`: `commissionLogId` als Prop

**Datei:** `src/app/creation/ai-editor-workspace.tsx`

Interface erweitern:
```ts
interface AIEditorWorkspaceProps {
  ...
  commissionLogId: number;   // ← neu
}
```

`handleSaveContent` und `handleSaveFromAI` übergeben `commissionLogId` an `POST /api/planning/history`:
```ts
body: JSON.stringify({
  keywordId,
  commissionLogId,           // ← neu
  contentBody: html,
  Event_Label: '...',
})
```

`handlePublish` übergibt `commissionLogId` an `PATCH /api/planning/keywords` via Custom-Header oder Body-Feld, damit die Route den `"Content veröffentlicht"`-Log mit der richtigen `commission_log_id` schreibt.

---

### Schritt 8 — `creation/page.tsx`: `commissionLogId` an Workspace übergeben

```tsx
<AIEditorWorkspace
  ...
  commissionLogId={selectedJob.commissionLogId}
/>
```

---

### Schritt 9 — `POST /api/planning/history`: `commission_log_id` speichern

**Datei:** `src/app/api/planning/history/route.ts`

Das POST-Body-Parsing wird um `commissionLogId` / `Commission_Log_Id` erweitert und an `createContentLog` übergeben.

---

### Schritt 10 — `PATCH /api/planning/keywords`: Veröffentlichungs-Log mit Zyklus-ID

**Datei:** `src/app/api/planning/keywords/route.ts`

Der PATCH-Body kann `commissionLogId` enthalten. Beim `Status === 'Published'`-Log-Eintrag wird es übergeben:

```ts
await createContentLog({
  ...
  Commission_Log_Id: updates.commissionLogId ?? null,
  Event_Label: 'Content veröffentlicht',
}, tenantId);
```

---

### Schritt 11 — `buildJobEntries`: Zyklus-basierte Status- und Content-Zuordnung

**Datei:** `src/app/creation/page.tsx`

Statt temporaler Nähe wird `commission_log_id` für die Zuordnung verwendet:

```ts
// Delivery: Log mit commission_log_id === commissioning.ID AND Event_Label === 'Content angeliefert'
const delivery = deliveryLogs.find(dl => dl.Commission_Log_Id === cl.ID);

// Per-job status: Hat dieser Zyklus einen 'Content veröffentlicht'-Log?
const publishLog = publishLogs.find(pl => pl.Commission_Log_Id === cl.ID);
const jobStatus = publishLog
  ? 'Published'
  : delivery
  ? kw?.Status  // live status for the active cycle
  : kw?.Status;
```

**Für den aktiven (neuesten) Zyklus** bleibt `kw.Status` die Quelle der Wahrheit (enthält `Beauftragt`, `In Arbeit`, `Angeliefert`, `Published`, etc.).

**Für abgeschlossene ältere Zyklen** (haben eigenen publish-Log via `commission_log_id`) wird `'Published'` angezeigt, unabhängig vom aktuellen `kw.Status`.

---

### Schritt 12 — Optisches Fix: "Optimierung"-Badge identisch mit "Erstellung"

**Datei:** `src/app/creation/page.tsx`

Indigo-Klasse vom Optimierung-Badge entfernen:
```diff
- job.actionType === 'Optimierung' && 'border-indigo-200 text-indigo-600 bg-indigo-50/50',
```

Beide Typen erscheinen dann optisch identisch als neutrales slate-Badge.

---

### Zusammenfassung der betroffenen Dateien

| Datei | Änderung |
|---|---|
| `src/lib/db/migrations/0005_add_commission_log_id.sql` | NEU — Migration |
| `src/lib/db/schema.ts` | `commissionLogId` auf `contentLog` |
| `src/lib/postgres-types.ts` | `Commission_Log_Id?` auf `ContentLog` |
| `src/lib/postgres.ts` | `createContentLog` + `mapContentLogRow` |
| `src/app/api/agent-webhook/trigger/route.ts` | Commission-Log-ID in Payload |
| `src/app/api/agent-webhook/callback/route.ts` | `commission_log_id` beim Delivery |
| `src/app/api/planning/history/route.ts` | `commission_log_id` in POST |
| `src/app/api/planning/keywords/route.ts` | `commission_log_id` beim Publish-Log |
| `src/app/creation/ai-editor-workspace.tsx` | `commissionLogId` als Prop + in Saves/Publish |
| `src/app/creation/page.tsx` | Zyklus-basierte Status- und Content-Zuordnung + Badge-Fix |

---

## Auftrags-Liste: Log-zentrierte Ansicht mit Paginierung

### Ist-Zustand
Die Auftragsliste in der Content-Erstellung zeigt **ein Row pro Keyword** (`commissionedKeywords` aus dem `keywords`-Array). Wenn dasselbe Keyword erst erstellt und später optimiert wird, erscheint es als einzelner Eintrag — der `Action_Type` wird vom neuesten Log abgeleitet.

### Soll-Zustand
**Ein Row pro Beauftragung** (d.h. pro `Event_Label === 'Content wurde beauftragt'`-Eintrag in `content_log`). Dadurch erscheinen Erstellung und Optimierung desselben Keywords als separate, historische Einträge.

Zusätzlich: **Paginierung** der Liste (z.B. 20 Einträge pro Seite) für bessere Ladeperformance.

### Datenmodell-Analyse
- `content_log` schreibt bei jeder Beauftragung einen Eintrag mit `Event_Label = 'Content wurde beauftragt'` und `Action_Type = 'Erstellung' | 'Optimierung'`.
- Bei Anlieferung: `Event_Label = 'Content angeliefert'`, `Version = 'v2'` (hat `Content_Body`).
- Die Creation Page lädt via `GET /api/planning/history` → `getAllContentHistory()` → `getContentLogs()` (limit 200).
- Die keywords-Liste wird via `GET /api/planning/keywords` separat geladen.

### Neues Konzept: "Auftrag" = zusammengesetztes Objekt aus Log + Keyword-Metadaten

Jeder Auftrag entspricht einem "Content wurde beauftragt"-Log-Eintrag. Für die Anzeige werden zusätzlich benötigt:
- **Keyword-Name** und **Target_URL** (aus dem keywords-Array per `keywordId`)
- **Action_Type** des Beauftragungslog (Erstellung/Optimierung)
- **Beauftragungszeitpunkt** (`Created_At` des Beauftragungslog)
- **Aktueller Status** des zugehörigen Keywords (`kw.Status`)
- **Lieferlog** (der zugehörige "Content angeliefert"-Eintrag für denselben Keyword-Zyklus, d.h. der nächste v2-Log nach dem Beauftragungszeitpunkt)

### Implementierungsplan

#### 1. Frontend-Datenstruktur: `JobEntry`
```typescript
interface JobEntry {
  commissionLogId: number;        // ID des "beauftragt"-Logs
  commissionedAt: string;         // Created_At des "beauftragt"-Logs
  keywordId: string;
  keyword: string;
  targetUrl?: string;
  actionType: 'Erstellung' | 'Optimierung';
  keywordStatus: KeywordStatus;   // aktueller Status aus keyword-record
  deliveryLogId?: number;         // ID des nächsten "Content angeliefert"-Logs nach Beauftragung
  isFailedRetry: boolean;
}
```

#### 2. Berechnung der `JobEntry`-Liste (Frontend, `creation/page.tsx`)
```
1. Filtere alle Logs auf Event_Label === 'Content wurde beauftragt' → commissioning logs
2. Sortiere absteigend nach Created_At
3. Für jeden commissioning log:
   a. Finde das zugehörige Keyword via keywordId
   b. Finde den nächsten 'Content angeliefert'-Log desselben Keywords 
      mit Created_At > commissioning.Created_At
   c. Baue JobEntry zusammen
4. Enthält keinen manuellen Filter auf kw.Status mehr — alle historischen Aufträge erscheinen
```

#### 3. Paginierung (Frontend)
- `PAGE_SIZE = 20`
- State: `const [jobPage, setJobPage] = useState(0)`
- Anzeige: `jobs.slice(jobPage * PAGE_SIZE, (jobPage + 1) * PAGE_SIZE)`
- Prev/Next-Buttons unterhalb der Liste

#### 4. Selektion anpassen
- Klick auf einen Row setzt `selectedJobEntry` (statt `selectedKeywordId`)
- Das rechte Panel lädt den Content des `deliveryLogId` wenn vorhanden
- `v2LogId = selectedJob?.deliveryLogId`

#### 5. `hasAnyHistory`-Logik für Failed-State entfällt
- Failed-State wird direkt auf den zugehörigen `keywordStatus === 'Planned'` des Auftrags geprüft, nicht mehr auf alle logs des keywords

#### 6. API: Paginierter History-Endpoint (optional, da limit 200 bereits existiert)
Aktuell lädt `/api/planning/history` alle 200 Logs auf einmal — das ist für den Start ausreichend. Die Paginierung erfolgt rein frontend-seitig im ersten Schritt. Wenn das Volumen wächst, kann ein `?page=&limit=`-Parameter ergänzt werden (Backend-Paginierung ist nicht Teil dieses Plans).

#### 7. Sonderfälle
- **Kein Keyword gefunden** (gelöscht): Zeige `"Gelöscht"` als Keyword-Name, `keywordStatus = 'Backlog'` als Fallback.
- **Kein Lieferlog** (noch in Arbeit): `deliveryLogId = undefined` → rechtes Panel zeigt Spinner.
- **Mehrere Beauftragungen** desselben Keywords: Jede erscheint als separater Row in der Liste.

---

## Bug A: Eintrag verschwindet nicht aus Redaktionsplan nach Beauftragung

### Root Cause
`plannedKeywords` in `editorial-planning.tsx:75` filtert auf `['Planned', 'Beauftragt', 'In Arbeit', 'Angeliefert', 'Review', 'Optimierung']`. Nach der Beauftragung wechselt der Status auf `Beauftragt` — der Eintrag bleibt also sichtbar (nur die Schaltfläche verwandelt sich in ein Badge).

### Fix
`plannedKeywords` soll nur `['Planned']` enthalten. Sobald ein Keyword beauftragt wurde, hat es den Status `Beauftragt` und gehört nicht mehr in den Redaktionsplan, sondern in die Content-Erstellungs-Liste.

**Datei:** `src/app/planning/editorial-planning.tsx:75`
```diff
- return keywords.filter(k => k.Status && ['Planned', 'Beauftragt', 'In Arbeit', 'Angeliefert', 'Review', 'Optimierung'].includes(k.Status));
+ return keywords.filter(k => k.Status === 'Planned');
```

---

## Bug B: Vorschau verschwindet nach "Veröffentlicht" — Spinner erscheint

### Root Cause (dreistufig)
1. `createContentLog` mit `Event_Label: 'Content veröffentlicht'` schreibt einen neuen Row in `content_log_body` mit `contentBody = null` und `eventLabel = 'Content veröffentlicht'`.
2. In `mapContentLogRow` gilt: `hasContent = body.hasBody` wobei `hasBody` per SQL `(contentLogBodyTable.contentLogId IS NOT NULL)` berechnet wird → `true` wenn eine Body-Row existiert, auch ohne `contentBody`. Damit ergibt sich `Version = 'v2'` für den "Content veröffentlicht"-Log-Eintrag.
3. Im Frontend sucht `v2Log = relevantLogs.find(log => log.Version === 'v2')` den **neuesten** v2-Log. Nach dem Publizieren ist das der "Content veröffentlicht"-Eintrag. Dessen Body wird on-demand geladen → `Content_Body = null` → `v2Content = ''` → Spinner.

### Fix: Zwei Ebenen
**Fix B1 — `Version`-Berechnung korrigieren (`postgres.ts`):**
`Version` soll nur `'v2'` sein wenn tatsächlich ein `Content_Body` vorhanden ist. `Event_Label`-only-Rows sollen `Version = 'v1'` erhalten.

```diff
- const hasContent = isPartial ? (body as any).hasBody : isFull ? !!(body as any).contentBody : false;
+ // hasBody in the partial case is the SQL IS NOT NULL check — but we only want v2 when
+ // there is actual HTML content, not just an event_label row without a body.
+ // So for partial rows we'd need a separate "hasActualBody" flag. Since we cannot
+ // change the SQL cheaply here, the safest fix is to add a "hasContentBody" column.
```

Actually, a simpler and safer approach: add a separate SQL computed flag `hasContentBody` to the list query that checks whether `content_body IS NOT NULL` (not just the JOIN row exists).

**Datei:** `src/lib/postgres.ts` — alle drei Stellen die `getContentLogs`, `getContentHistoryByKeyword`, `getContentHistoryByUrl` aufrufen. In allen three queries muss das `hasBody`-Flag auf `content_body IS NOT NULL` statt auf `contentLogBodyTable.contentLogId IS NOT NULL` umgestellt werden.

**Fix B1 — `hasBody`-SQL-Ausdruck in `postgres.ts` korrigieren (4 Stellen):**

Die 4 List-Queries verwenden `(contentLogBodyTable.contentLogId IS NOT NULL)` als `hasBody`-Flag. Dieser Ausdruck ist `true` sobald eine Body-Row existiert — auch wenn `content_body` selbst `NULL` ist (d.h. reine Event-Label-Einträge). Korrekt wäre `(contentLogBodyTable.content_body IS NOT NULL)`.

Alle 4 Stellen in `src/lib/postgres.ts` ändern:
```diff
- hasBody: drizzleSql<boolean>`(${contentLogBodyTable.contentLogId} IS NOT NULL)`,
+ hasBody: drizzleSql<boolean>`(${contentLogBodyTable.contentBody} IS NOT NULL)`,
```

Betroffene Funktionen:
- `getContentLogs` (Zeile 528)
- `getContentHistoryByUrl` (Zeile 550)
- `getContentHistoryByUrlOrKeywords` (Zeile 575)
- `getContentHistoryByKeyword` (Zeile 601)

---

## Fehlerursachen (vorherige Analyse)
1.  **Typ-Verlust in der Planung:** Der "Hinzufügen"-Button im Vorschläge-Tab übernimmt den `Action_Type` nicht immer korrekt bzw. die API setzt ihn teilweise hart zurück.
2.  **Harter Status-Sprung:** Der Agent-Trigger setzt den Status sofort auf `Beauftragt`, was die Unterscheidung zwischen "in Planung" und "wirklich beauftragt" erschwert, wenn die UI nicht synchron ist.
3.  **Anzeige-Logik in der Content-Erstellung:** Die Liste in der Content-Erstellung filtert teilweise unsauber und greift für den Typ ("Erstellung"/"Optimierung") auf Log-Einträge zu, die bei parallelen oder aufeinanderfolgenden Aufträgen zu Verwirrung führen können.
4.  **Historien-Überschreibung:** Wenn ein Thema erneut beauftragt wird, wird oft der `Action_Type` auf dem Keyword-Record geändert, was die Sicht auf vergangene Aktionen verfälscht, wenn die Log-Einträge nicht sauber getrennt sind.

## Ziele
1.  **Saubere Typ-Mitführung:** Der `Action_Type` ("Erstellung"/"Optimierung") wird vom Vorschlag über die Planung bis zur Beauftragung strikt beibehalten.
2.  **Workflow-Integrität:** Einträge verschwinden erst bei echtem Status-Wechsel (`Beauftragt`) aus dem Redaktionsplan.
3.  **Eindeutige Auftrags-Liste:** In der "Content-Erstellung" wird pro Beauftragung ein klarer Zustand angezeigt, basierend auf dem aktuellsten Log-Eintrag dieses Typs.

## Geplante Schritte

### 1. API-Anpassungen (Keywords & Trigger)
*   **`src/app/api/planning/keywords/route.ts` (PATCH)**: Sicherstellen, dass `Action_Type` niemals implizit geändert wird, außer es wird explizit im Payload mitgeschickt. Die automatische Rücksetzung auf "Erstellung" bei `Status: 'Planned'` wird entfernt.
*   **`src/app/api/agent-webhook/trigger/route.ts`**: Den `Action_Type` für das Logging und den Workflow-Payload strikt aus dem Request-Body oder dem aktuellen Datenbank-Zustand ableiten.

### 2. UI-Anpassungen (Redaktionsplan & Vorschläge)
*   **`src/features/planning/components/suggestion-columns.tsx`**: Der "Hinzufügen"-Button übergibt den `Action_Type` des Vorschlags explizit an die API.
*   **`src/app/planning/editorial-planning.tsx`**: Die `handleCommissionContent`-Funktion muss den `Action_Type` des Keywords an den `triggerN8nAction`-Aufruf übergeben, damit der Agent weiß, ob er erstellen oder optimieren soll.

### 3. Content-Erstellung Liste (UI-Fix)
*   **`src/app/creation/page.tsx`**: Die Anzeige des Typs ("Erstellung" oder "Optimierung") in der Liste links wird so angepasst, dass sie den `Action_Type` der **aktuellen Beauftragung** (aus dem Status oder dem neuesten Log) anzeigt, statt hart auf den ersten Log-Eintrag zu schauen.

### 4. Datenmodell & Logging
*   Verifizierung, dass `createContentLog` bei jeder Beauftragung einen neuen Eintrag erzeugt. Das ist technisch bereits so, aber die UI muss lernen, diese Einträge als separate "Events" in der Historie zu behandeln (was sie durch die `id` bereits tut).

## Validierung
1.  **Szenario Ersterstellung:** Hinzufügen -> Beauftragen -> Erscheint als "Erstellung" in Liste? -> OK.
2.  **Szenario Optimierung:** (Nach Veröffentlichung) -> Optimierung vorschlagen -> Erscheint in Vorschläge? -> Hinzufügen -> Erscheint im Redaktionsplan als "Optimierung"? -> Beauftragen -> Erscheint als "Optimierung" in Liste? -> OK.
3.  **Historie-Check:** Zeigt die Detail-Ansicht beide Aktionen (Erstellung UND Optimierung) separat an? -> OK.




---
*Erstellt am 15.05.2026*

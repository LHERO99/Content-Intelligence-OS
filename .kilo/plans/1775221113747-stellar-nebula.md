# Implementation Plan: Content Workspace Fixes

## Goal
Fix commission timestamp formatting and add HTML formatting preview/H-tag editing capabilities in the Content Creation workspace.

## Tasks

### 1. Fix Commission Timestamp
- Analyze `src/app/creation/ai-editor-workspace.tsx` and the relevant component that renders the "Aufträge" list to identify where the "Beauftragt: gerade eben..." string is generated.
- Update the formatting logic to ensure all timestamps follow the standard `DD.MM.YYYY, HH:MM` format.

### 2. Improve HTML Editing & Preview
- Investigate the current implementation of the Tiptap editor in `src/app/creation/ai-editor-workspace.tsx`.
- Enable/Fix H1, H2, H3 tags in the Tiptap toolbar/menu.
- Add a dedicated view mode or split-pane view that allows users to see the raw HTML source or a rendered "formatted" view to facilitate checking H-tag structure.

## Plan
1.  **Investigation**: Read `src/app/creation/ai-editor-workspace.tsx` and related components to understand how the commission list and editor are currently implemented.
2.  **Implementation**:
    - Update timestamp formatting in the commission list component.
    - Enhance Tiptap editor configuration to properly expose H1-H3 tags.
    - Add a button or toggle to switch between the editor and an HTML source/preview pane.
3.  **Verification**: Test both fixes in the local environment and verify correct rendering and formatting.

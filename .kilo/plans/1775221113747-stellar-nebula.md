# Implementation Plan: Content Workspace Fixes (Revised)

## Goal
Fix commission timestamp formatting, integrate HTML source view directly into the "Edit" tab, resolve CSS/Editor issues preventing H-tags from being visually distinct, and refine UI colors for a cleaner, more professional look.

## Tasks

### 1. Fix Commission Timestamp & immediate Display (Verify)
- Ensure timestamps follow `DD.MM.YYYY, HH:MM` format in `src/app/creation/page.tsx`.

### 2. UI Color Refinement
- **Commission Timestamp**: Change the color from emerald green to a neutral muted/gray color (e.g., `text-muted-foreground` or `text-slate-500`) in the "Aufträge" list.
- **Action Type Badge (e.g., ERSTELLUNG)**: Change the badge styling to a subtle gray tone (e.g., `bg-slate-100 text-slate-600 border-slate-200`) instead of the current emerald-tinted version.

### 3. Editor UI & HTML Toggle (Verify)
- Ensure `src/app/creation/rich-text-editor.tsx` includes the "Code View" toggle within its toolbar.

### 4. Fix Visual Formatting (H-Tags & Paragraphs) (Verify)
- Ensure H1, H2, H3, and P tags have clear visual distinction and margins in the editor.

## Verification
- Confirm the "Beauftragt" text is no longer green.
- Confirm the "Erstellung/Optimierung" badge is subtle gray.
- Verify editor formatting and HTML toggle.

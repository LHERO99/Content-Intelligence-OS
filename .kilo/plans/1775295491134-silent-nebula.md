# Plan: Content-Historie & UI-Optimierung

Dieses Dokument beschreibt die Erweiterung der Content-Historie um neue Event-Typen und die Verbesserung der Timeline-UI.

## 1. Datenmodell & Events

Die Historie soll URL-basiert (gruppiert nach `Target_URL`) folgende Ereignisse erfassen:

| Event | Action_Type (Airtable) | Diff_Summary (Vorschlag) | Trigger |
| :--- | :--- | :--- | :--- |
| URL zur Keyword-Map hinzugefügt | Planung | URL zur Keyword-Map hinzugefügt | `POST /api/planning/keywords` |
| URL in Vorschläge aufgenommen | Planung | URL in Vorschläge aufgenommen | TBD (Statusänderung?) |
| URL in Redaktions-Planung aufgenommen | Planung | URL in Redaktions-Planung aufgenommen | Statusänderung -> `Planned` |
| Content beauftragt | Erstellung | Content beauftragt | `POST /api/n8n/trigger` (Action: COMMISSION_CONTENT) |
| Content erstellt | Erstellung | Content erstellt (v1/v2) | `POST /api/n8n/callback` |
| URL zur Optimierung in Vorschläge aufgenommen | Optimierung | URL zur Optimierung aufgenommen | TBD |
| Content beauftragt (Optimierung) | Optimierung | Content-Optimierung beauftragt | TBD |
| Content optimiert | Optimierung | Content optimiert | TBD |
| Content veröffentlicht | Optimierung | Content erfolgreich veröffentlicht | Statusänderung -> `Published` |

## 2. UI-Optimierung (Pop-Up / HistoryList)

Das Ziel ist eine übersichtlichere Timeline im Pop-up:

- **Scrollbarkeit**: Der Container erhält eine feste Maximalhöhe mit `overflow-y-auto`. Die neuesten Einträge stehen oben.
- **Timeline Design**:
  - Vertikale Linie links.
  - Icons für verschiedene Event-Typen (Lucide-Icons: `PlusCircle`, `Lightbulb`, `Calendar`, `Send`, `FileText`, `RefreshCw`).
  - **Collapsible Content**: Bei "Content erstellt/optimiert" soll der `Content_Body` standardmäßig eingeklappt sein und per Klick/Chevron erweitert werden können.
- **Gruppierung**: Da die Historie URL-basiert ist, wird sichergestellt, dass im Pop-up alle Events zu dieser URL (auch von verschiedenen Keywords, falls zutreffend) angezeigt werden.

## 3. Umsetzungsschritte

### Phase 1: Tracking-Erweiterung (Backend)
- [ ] **Keyword Creation**: `POST /api/planning/keywords` anpassen, um initiale Erstellung zu loggen.
- [ ] **Status Transitions**: In `PATCH /api/planning/keywords` feinere Logik für die neuen Status-Events implementieren (z.B. Optimierungsvorschlag).
- [ ] **Optimierung**: Sicherstellen, dass Optimierungs-Workflows ebenfalls `createContentLog` aufrufen.

### Phase 2: UI-Refactoring (`HistoryList.tsx`)
- [ ] **Scroll-Area**: `ScrollArea` (Radix UI) oder CSS `max-h` integrieren.
- [ ] **Timeline Layout**: Neues Layout mit vertikaler Linie und Icons umsetzen.
- [ ] **Collapsible**: `Content_Body` in ein `Accordion` oder ein einfaches State-basiertes Toggle-Element auslagern.
- [ ] **Styling**: Tailwind-Klassen für DocMorris Branding (`#00463c`) beibehalten/verfeinern.

### Phase 3: Globale Historie (`ContentHistoryTable.tsx`)
- [ ] Abgleich der UI-Änderungen von `HistoryList` mit der Detail-Ansicht in der globalen Tabelle, um Konsistenz zu wahren.

## 4. Offene Fragen
- Gibt es spezifische Status-Namen in Airtable für "URL in Vorschläge aufgenommen"? (Aktuell: `Backlog` -> `Planned`).
- Sollen wir den `Content_Body` auch in der globalen Tabelle standardmäßig einklappen? (Aktuell ist er dort in einem Scroll-Container).
